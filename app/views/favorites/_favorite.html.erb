<span>
  <% if post_image.favorited_by?(current_user) %>
    <%= link_to post_image_favorites_path(post_image), method: :delete, remote: true, class:"text-dark" do %>
      <i class="fas fa-heart text-danger"></i>️ <%= post_image.favorites.count %>
    <% end %>
  <% else %>
    <%= link_to post_image_favorites_path(post_image), method: :post, remote: true, class:"text-dark" do %>
      <i class="fas fa-heart"></i> <%= post_image.favorites.count %>
    <% end %>
  <% end %>
</span>